import streamlit as st
import pandas as pd
from datetime import datetime, timedelta
import numpy as np

st.title("📊 Тест пояснений для графиков")

# Демонстрация разных типов пояснений
st.header("Примеры пояснений:")

st.subheader("1. Funnel Chart пояснение:")
st.info("""
**📊 Как читать Funnel Chart:**
• **Общая конверсия** — показывает эффективность всей воронки за весь период данных  
• **Drop-off между шагами** — где теряем больше всего пользователей (приоритет для оптимизации)  
• **Объемы на каждом шаге** — помогают планировать ресурсы и прогнозировать результаты  

💡 *Эти метрики агрегированы за весь период и могут отличаться от временных трендов в Time Series*
""")

st.subheader("2. Flow Diagram пояснение:")
st.info("""
**🌊 Как читать Flow Diagram:**
• **Толщина потоков** — пропорциональна количеству пользователей (где самые большие потери?)  
• **Визуальные узкие места** — сразу видно проблемные переходы в воронке  
• **Альтернативный взгляд** — та же статистика что в Funnel Chart, но в формате Sankey  

💡 *Удобно для презентаций stakeholders и выявления критических точек потерь*
""")

st.subheader("3. Time Series пояснение:")
st.info("""
**📈 Как читать Time Series:**
• **Временные тренды** — видите динамику изменений конверсий по периодам  
• **Сезонность и аномалии** — выявляете паттерны роста/падения для принятия решений  
• **Точные конверсии по периодам** — каждая точка = конверсия только в этом периоде (≤100%)  

⚠️ *Конверсии могут отличаться от Funnel Chart, т.к. здесь считается по периодам, а не за всё время*
""")

st.header("Демонстрация различий метрик:")

# Создаем пример данных, показывающих почему метрики различаются
col1, col2 = st.columns(2)

with col1:
    st.subheader("📊 Funnel Chart логика")
    st.markdown("""
    **Агрегация за весь период:**
    - Всего стартовало: 1000 пользователей
    - Завершили: 150 пользователей  
    - **Общая конверсия: 15%**
    """)
    
    st.dataframe({
        "Метод": ["Funnel Chart"],
        "Период": ["Весь период (7 дней)"],
        "Стартовали": [1000],
        "Завершили": [150], 
        "Конверсия": ["15%"]
    })

with col2:
    st.subheader("📈 Time Series логика")
    st.markdown("""
    **Разбивка по дням:**
    - День 1: 10 стартовали, 5 завершили = 50%
    - День 2: 200 стартовали, 20 завершили = 10%
    - День 3: 300 стартовали, 45 завершили = 15%
    - **Средняя дневная конверсия: 25%**
    """)
    
    st.dataframe({
        "День": [1, 2, 3],
        "Стартовали": [10, 200, 300],
        "Завершили": [5, 20, 45],
        "Конверсия": ["50%", "10%", "15%"]
    })

st.warning("""
🎯 **Ключевая разница:** Funnel Chart показывает общую эффективность воронки, 
а Time Series — как конверсия менялась по времени. Оба взгляда важны для принятия решений!
""")

st.success("""
✅ **Рекомендации для анализа:**
- Используйте Funnel Chart для общей оценки и выявления узких мест
- Используйте Time Series для поиска трендов и сезонности  
- Сравнивайте метрики в контексте, а не абсолютно
""")
